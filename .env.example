# Puerto del servidor
PORT=3000

# Credenciales del administrador (se crean al iniciar la BD por primera vez)
ADMIN_USER=octavio
ADMIN_PASS=Hypercix2026.

# ─── BASE DE DATOS ─────────────────────────────────────
# DB_DRIVER=sqlite  → archivo local database/hypercix.db (por defecto)
# DB_DRIVER=mysql   → MySQL/MariaDB (VPS, Contabo, etc.)
DB_DRIVER=sqlite

# Solo se usan si DB_DRIVER=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=hypercix
DB_PASS=TuClaveSegura123
DB_NAME=hypercix
